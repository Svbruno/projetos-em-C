#include <stdio.h>
int main() {

printf("tamanho");
scanf("%", &num);
for (int i = 0; i < num; i++){
for (int j = 0; j < num; j++){
    if( i <= i){
        printf("%d", i + 1);
    } else {
        printf("0");
    }
}

printf("\n");
}
    return 0;
}